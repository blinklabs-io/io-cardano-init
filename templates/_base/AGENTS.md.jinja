# AGENTS.md — {{ project_name }}

This is a Cardano protocol project generated by [`cardano-init`](https://github.com/input-output-hk/cardano-init). The components below were wired together through a fixed **interface contract** (see *Golden rules*) — they compose through that contract, not through direct imports across directories.

## Stack

| Role | Tool | Language | Directory |
|------|------|----------|-----------|
{%- if has_fullstack %}
| Protocol (on-chain + off-chain) | {{ fullstack.tool_name }} | {{ fullstack.language }} | `{{ fullstack.dir }}/` |
{%- endif %}
{%- if has_on_chain %}
| On-chain | {{ on_chain.tool_name }} | {{ on_chain.language }} | `{{ on_chain.dir }}/` |
{%- endif %}
{%- if has_off_chain %}
| Off-chain | {{ off_chain.tool_name }} | {{ off_chain.language }} | `{{ off_chain.dir }}/` |
{%- endif %}
{%- if has_devnet %}
| Devnet | {{ devnet.tool_name }} | {{ devnet.language }} | `{{ devnet.dir }}/` |
{%- endif %}
{%- if has_infra %}
| Infrastructure | {% for t in infra_tools %}{{ t.tool_name }}{% if not loop.last %}, {% endif %}{% endfor %} | — | `infra/` |
{%- endif %}
{%- if has_formal_methods %}
| Formal methods | {{ formal_methods.tool_name }} | {{ formal_methods.language }} | `{{ formal_methods.dir }}/` |
{%- endif %}

## Golden rules (do not break these)

These invariants are what let the components compose. Preserve them in any change:

1. **Blueprint is the on-chain → off-chain contract.** The {% if has_fullstack %}`{{ fullstack.dir }}/`{% elif has_on_chain %}`{{ on_chain.dir }}/`{% else %}on-chain{% endif %} component's `build` writes the CIP-57 Plutus blueprint to **`blueprint/plutus.json`**. Every other component reads that file — never reach into the on-chain source to get script bytes or parameter shapes.
2. **`plutus.json` may be absent.** It only exists after an on-chain `build`. Consumers must **degrade gracefully** when it is missing, not crash.
3. **`.env` is the connection seam — never hand-wire it.** Whichever component provisions a local endpoint writes the standard vars (`INDEXER_URL`, `OGMIOS_URL`, `NODE_SOCKET_PATH`, …) into the root `.env` during its `dev`. Consumers read those keys and **fall back gracefully when blank** (e.g. to a public provider). Do not add bespoke cross-component config; react to the presence of a key, never to which tool set it.
4. **Every component is standalone and composable.** Each component `Justfile` exposes `build`, `test`, `clean` and must succeed on its own. `dev` is **optional and per-component** (only where a real watch/daemon/devnet mode exists) and is **never** aggregated at the top level.
5. **Components talk to the contract, not to each other.** There is no direct dependency between, say, `{% if has_off_chain %}{{ off_chain.dir }}/{% else %}off-chain/{% endif %}` and `{% if has_on_chain %}{{ on_chain.dir }}/{% else %}on-chain/{% endif %}` — only through `blueprint/` and `.env`.

## Layout

```
{{ project_name }}/
├── Justfile                    # Top-level task orchestration (build/test/clean)
├── AGENTS.md / CLAUDE.md       # This guidance
├── .env                        # Shared connection vars (the .env seam)
{%- if has_fullstack %}
├── {{ fullstack.dir }}/                  # {{ fullstack.tool_name }} — fullstack: on-chain + off-chain in one build
{%- endif %}
{%- if has_on_chain %}
├── {{ on_chain.dir }}/                  # {{ on_chain.tool_name }} — validators / smart-contract logic (produces the blueprint)
{%- endif %}
{%- if has_off_chain %}
├── {{ off_chain.dir }}/                 # {{ off_chain.tool_name }} — transaction building & submission
{%- endif %}
{%- if has_infra %}
├── infra/                      # Infrastructure services (via cardano-up)
{%- endif %}
{%- if has_devnet %}
├── {{ devnet.dir }}/                     # {{ devnet.tool_name }} — local throwaway chain
{%- endif %}
{%- if has_formal_methods %}
├── {{ formal_methods.dir }}/          # {{ formal_methods.tool_name }} — specification & verification
{%- endif %}
{%- if has_blueprint %}
├── blueprint/                  # CIP-57 Plutus blueprint (plutus.json lands here after on-chain build)
{%- endif %}
└── README.md
```

## Commands

Everything is driven by [`just`](https://just.systems). Run these from the root:

```bash
just build    # Build all components (on-chain / protocol first, so the blueprint exists)
just test     # Build the blueprint, then run each component's tests in role order
just clean    # Remove build artifacts (and blueprint/plutus.json)
```
`build`, `test`, and `clean` are the only **aggregated** targets — they terminate and compose. Long-running or interactive modes live per component and are **not** aggregated. Discover and run them directly:

```bash
just -f <dir>/Justfile --list     # see a component's targets
just -f <dir>/Justfile dev        # run its dev/watch/devnet mode (only where it exists)
```
{%- if has_devnet %}

`just -f {{ devnet.dir }}/Justfile dev` starts a persistent local devnet and writes its connection details into `.env`; off-chain/consumer components then pick it up automatically. `just test-integration` spins the devnet up, runs a smoke test, and tears it down.
{%- endif %}

## Components

{%- if has_fullstack %}

### Protocol — {{ fullstack.tool_name }} (`{{ fullstack.dir }}/`, {{ fullstack.language }})

A single {{ fullstack.language }} component implementing **both** on-chain validators and off-chain transaction logic. The two halves share types in-process, but the external seam still holds: `just build-protocol` writes `blueprint/plutus.json` (so devnet/formal-methods/infra compose) and it reads/writes `.env` like any off-chain consumer. Official docs: {{ fullstack.website }}
{%- endif %}
{%- if has_on_chain %}

### On-chain — {{ on_chain.tool_name }} (`{{ on_chain.dir }}/`, {{ on_chain.language }})

Smart-contract / validator logic. Its `build` is the **producer** of `blueprint/plutus.json`; keep the blueprint's script names and parameter shapes stable, since off-chain code binds to them. Official docs: {{ on_chain.website }}
{%- endif %}
{%- if has_off_chain %}

### Off-chain — {{ off_chain.tool_name }} (`{{ off_chain.dir }}/`, {{ off_chain.language }})

Transaction building & submission. It **consumes** `blueprint/plutus.json` and reads `.env` for its chain connection (falling back to a public provider when the local vars are blank). Official docs: {{ off_chain.website }}
{%- endif %}
{%- if has_devnet %}

### Devnet — {{ devnet.tool_name }} (`{{ devnet.dir }}/`, {{ devnet.language }})

A local throwaway chain for development and integration tests. Its `dev` is a **provider** of the `.env` connection vars. Official docs: {{ devnet.website }}
{%- endif %}
{%- if has_infra %}

### Infrastructure — `infra/` (via cardano-up)

Provisions the selected services and publishes their connection details to `.env`:
{% for t in infra_tools %}
- **{{ t.tool_name }}** — {{ t.website }}
{%- endfor %}

Requires Docker. See `infra/README.md` for the `just` targets.
{%- endif %}
{%- if has_formal_methods %}

### Formal methods — {{ formal_methods.tool_name }} (`{{ formal_methods.dir }}/`, {{ formal_methods.language }})

Specification & verification. `just verify` runs the checks; it is part of the aggregated `just test`. Official docs: {{ formal_methods.website }}
{%- endif %}

## The worked example

Every generated project ships the **same** worked example — a **gift card**: a one-shot minting policy that mints a unique token gated by a specific UTxO, plus a `redeem` validator that releases a locked gift when the token is burned. On- and off-chain sides share the same parameter ABI through the blueprint, which is why the project builds and its tests pass out of the box. Use it as the template for real logic; replace it, don't accrete around it.

## Cardano knowledge: use cardano-dev-skills

For Cardano-specific work (writing validators, building transactions, debugging on-chain failures, tokens, governance), consult the [**cardano-dev-skills**](https://github.com/cardano-foundation/cardano-dev-skills) skill set rather than relying on model training data — Cardano tooling drifts fast. In Claude Code, install once:

```
/plugin marketplace add cardano-foundation/cardano-dev-skills
/plugin install cardano-dev-skills@cardano-dev-skills
```

Then run `/cardano-context` to persist Cardano directives into this project. (Other agents: read the `skills/*/SKILL.md` files directly, or symlink `skills/` into `.agents/skills`.)

**Skills most relevant to this stack:**

- `suggest-tooling`, `explain-eutxo`, `explain-cip` — orientation and the eUTxO model
{%- if has_on_chain or has_fullstack %}
- `write-validator`, `review-contract`, `optimize-validator` — for the {% if has_fullstack %}`{{ fullstack.dir }}/`{% else %}`{{ on_chain.dir }}/`{% endif %} validator work ({% if has_fullstack %}{{ fullstack.tool_name }}{% else %}{{ on_chain.tool_name }}{% endif %})
{%- endif %}
{%- if has_off_chain or has_fullstack %}
- `build-transaction`, `debug-transaction` — for the {% if has_fullstack %}`{{ fullstack.dir }}/`{% else %}`{{ off_chain.dir }}/`{% endif %} off-chain work ({% if has_fullstack %}{{ fullstack.tool_name }}{% else %}{{ off_chain.tool_name }}{% endif %})
- `connect-wallet` — CIP-30 wallet integration if you add a dApp frontend
{%- endif %}
{%- if has_devnet or has_infra %}
- `setup-devnet` — local devnet / testnet setup and connection
{%- endif %}
{%- if has_off_chain or has_infra or has_fullstack %}
- `query-chain` — choosing how to read chain data (Blockfrost / Ogmios / indexers)
{%- endif %}

## Gotchas

- **Don't hand-edit connection URLs in `.env`.** They're populated at runtime by whichever component provides a local endpoint (see Golden rule 3). Blank is a valid state — it means "use the public fallback."
- **`blueprint/plutus.json` is a build artifact**, not source. It won't exist until an on-chain build has run; regenerate it with `just build` rather than editing it by hand.
- **Keep the interface contract intact.** If you add or swap a component, make it read/write the same `blueprint/` and `.env` keys — that's what keeps the stack composable without per-pair glue.
- **Don't add a top-level `dev` target** or make one component import another's source. Cross-component coupling belongs in the contract, not the code.
